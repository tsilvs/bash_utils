# Data

+ [`path-map`](./path-map.json) - an attempt to map Linux config path associations. Some paths serve identical functions in different contexts (per user or system-wide) and follow the same specs (subdirectory tree structure, file formats, data schemas).
